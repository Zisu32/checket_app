name: checket
description: Smart Wardrobe System Checket
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  drift: ^2.21.0
  drift_flutter: ^0.3.1
  sqlite3: ^3.0.0
  supabase_flutter: ^2.8.0
  qr_flutter: ^4.1.0
  uuid: ^4.5.1
  crypto: ^3.0.6
  web: ^1.1.1
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  drift_dev: ^2.21.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
